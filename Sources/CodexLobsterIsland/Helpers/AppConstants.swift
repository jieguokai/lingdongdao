import CoreGraphics

enum AppConstants {
    static let compactIslandSize = CGSize(width: 364, height: 38)
    static let expandedIslandSize = CGSize(width: 580, height: 310)
    static let islandTopMargin: CGFloat = 18
}
