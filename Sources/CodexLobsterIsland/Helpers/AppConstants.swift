import CoreGraphics

enum AppConstants {
    static let compactIslandSize = CGSize(width: 212, height: 31)
    static let expandedIslandSize = CGSize(width: 440, height: 320)
    static let islandTopMargin: CGFloat = 7
}
