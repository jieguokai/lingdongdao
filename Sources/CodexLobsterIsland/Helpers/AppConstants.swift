import CoreGraphics

enum AppConstants {
    static let compactIslandSize = CGSize(width: 380, height: 84)
    static let expandedIslandSize = CGSize(width: 470, height: 310)
    static let islandTopMargin: CGFloat = 18
}
